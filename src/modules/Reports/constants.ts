import { buildRouteTree } from 'shared/helpers/buildRouteTree';

export const routes = buildRouteTree({
  reports: {
    'open-orders': null,
    'order-history': null,
    'trade-history': null,
    'mobile': null,
  },
});
